package com.example.parasuratanuser;
/*
public class Library extends JFrame {
    private JPanel contentPanel;

    public Library() {
        // initialize components and layout
        initComponents();
        loadData(); // load initial data
    }

    public void loadData() {
        // Clear and reload data in the panel or table
        contentPanel.removeAll();

        // Example: Load updated items (re-fetch from source)
        List<Item> items = ItemDatabase.getAllItems(); // replace with your data source
        for (Item item : items) {
            JLabel itemLabel = new JLabel(item.getName());
            contentPanel.add(itemLabel);
        }

        contentPanel.revalidate();
        contentPanel.repaint();
    }
}
*/